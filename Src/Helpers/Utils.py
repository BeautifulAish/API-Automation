# Common Headers
def common_headers_json():
    Headers = {
        "Content-type" = "application/json",
    }
    return headers


def common_headers_xml():
    Headers = {
        "Content-type" = "application/xml",
    }
    return Headers

# Read data from Excel,CSV, json,Xml,YAML - Keep the functions in future
